# Agent 接入、更新与迁移指南

本文件是接入流程的唯一说明。运行时内容格式与操作分别由 `skills/kb-ingest/SKILL.md`、`skills/kb-search/SKILL.md` 定义。无需安装器、项目依赖或 API Key。

## 确定目标

先确定用户指定的项目根目录和客户端；环境已明确时不重复询问，缺失时询问。后续路径均相对此根目录，而非当前 shell 子目录或模板仓库。读取适用项目规则，检查已有文件、覆盖规则、符号链接与目标实际路径；不越出用户指定项目修改其他目录。知识库固定为根目录的 `knowledge/`。

| 客户端 | 复制目标 | 根规则 |
| --- | --- | --- |
| Codex | `.agents/skills/kb-search/SKILL.md`、`.agents/skills/kb-ingest/SKILL.md` | `AGENTS.md` |
| Claude Code | `.claude/skills/kb-search/SKILL.md`、`.claude/skills/kb-ingest/SKILL.md` | `CLAUDE.md` |

默认只安装指定客户端。明确同时使用两者时复制相同正文至两处，分别维护根规则。实际复制，不使用符号链接、不依赖模板原位置，不修改全局目录、插件或 MCP 配置；无需给用户项目初始化 Git。

## 首次接入

1. 比较目标现状，保留所有原有项目文件。
   - 无 `knowledge/`：复制模板的 `knowledge/`，只有空索引和概念目录。
   - 已是本规格：保留条目、概念和索引，不覆盖空模板。
   - 是旧版：转入下方一次性迁移流程，仅在已有迁移授权范围内执行。
   - 无关同名目录或归属不明：保留，说明冲突并询问，不能直接覆盖或擅自转换。
2. 逐一比较两个 skill 的目标路径。不存在则复制；正文相同不重写。现有同名 skill 只有能确认是未修改的本模板旧版本、且本次授权包含更新时才替换；无法确认归属或有用户修改时保留原件，展示具体差异，待明确取舍后再处理。其他 skill 不变。
   如果客户端沙箱禁止写项目 skill 目录，使用当前环境已授权的文件操作完成复制，或明确报告尚未放置的路径；不要改装到全局目录或普通子目录来绕过发现机制，也不要将部分接入报为成功。
3. 保留现有根规则，在末尾追加下列一句；文件不存在则创建。已有语义等价且指向本项目知识库的规则不重复追加。

   ```text
   当用户要求查询知识库，或当前任务需要参考本项目已收录的资料、概念和经验时，使用 kb-search skill，从本项目根目录的 knowledge/_index.md 开始检索，并在回答中标明所用条目来源。
   ```

4. 检查规则确实适用。Codex 同目录的 `AGENTS.override.md` 优先于 `AGENTS.md`，还需检查执行子目录的更具体规则、客户端配置的规则入口与读取限制。Claude Code 检查实际加载的 `CLAUDE.md`、导入和上下级规则。存在覆盖或冲突，说明具体文件及影响，不擅自改写其他规则，不把追加成功当作生效。
5. 使用当前客户端确认实际发现与加载：在目标项目开启新会话；Codex 查看 skill 列表（可用客户端的 `/skills`）并调用两个 skill；Claude Code 查看 `/` 列表中的两个 skill，使用 `/memory` 检查规则入口、`/context` 确认实际加载。还需核对选中的 skill 来源：Claude Code 的个人或企业同名 skill 可能优先于项目级，Codex 的同名 skill 也可能同时出现；报告冲突，不改全局文件。版本界面有差异时使用其实际提供的发现方式。不要关闭发现功能后测试，也不要仅手动读取 skill 文件就声称自动发现成功。
6. 在该会话查询知识库，确认读取目标索引、空库返回空库且无文件变化。用户真实空库不写测试知识；首次入库由用户提供材料。客户端未加载时，明确需要在目标项目重开会话或处理的规则入口，状态为“文件已放置，客户端接入未验证”。
7. 返回知识库绝对路径、两个 skill 路径、修改的规则文件，以及实际验证或未验证的结果。

从子目录启动也必须使用同一接入项目的根目录。Codex 在没有 Git 根目录的项目中可能只发现启动目录的规则与 skill（本地 0.153.4 已复现）；这类项目从目标根目录开启会话，例如 `codex --cd "用户指定的项目根目录"`，再在会话内操作子目录。无需初始化 Git。已在子目录启动且目标已明确时可按该根目录读取项目 skill 完成当前请求，但这不能证明启动时的自动发现与根规则加载成功；应从根目录重开会话确认，不创建子目录知识库。

## 重复接入与更新

重复接入仍按上述比较流程执行，内容不变则不写；用户知识、其他项目文件和自定义 skill 必须保留，读取规则只一份。

模板更新只涉及两个 skill 与必要的根规则，不复制空索引，不重写知识或顺带补做全库导航。用户修改过的 skill 先比较差异并明确取舍；冲突未处理时报告局部完成。内容合同确实改变才执行一次性迁移，不在两个运行时 skill 中增加新旧格式分支。

## 旧版一次性迁移

旧版基线为 `cb5877e11122f926de4725b60c4efd9e11275f46`。新版退出旧 `/kb` 入口、Python CLI、`KB_ROOT`、转写配置及工具依赖；原来的命令将不再可用。两个新 skill 承接检索与收录，入库时完成导航，无独立编译或修复流程。此取舍不授权删除用户材料，不迁移其他项目或全局旧 skill。

仅对用户授权的目标执行以下操作；迁移前先阅读新入库 skill 的内容合同。

1. 盘点条目、概念、内部原文、附件、收件箱、报告、旧 skill、根规则及目录级规则。记录原文位置、用户思考和人工概念内容；分辨示例和真实资料，不把历史输出自动当作权威知识。
2. 在 `knowledge/` **之外**创建本次唯一备份目录，完整复制旧知识库、项目级 `.claude/skills/kb/` 和将修改的规则文件（保留相对目录结构）。回读/比较备份，向用户记录实际位置；备份不得作为新版搜索范围。确认备份可用后才修改。迁移中用备份保留退出的文件，不以硬删除或覆盖作迁移手段。
3. 沿用条目路径和首次收录日期，转为带引号的 `date` 和字符串 `source`；保留有用 `tags` 和确实存在的 `source_url`。作者、材料日期及其他有意义元数据转入来源说明，退出 `confidence`、`source_type` 等旧状态/分类字段与脚本生成的 `concepts`、`related` 冗余字段；有效关系转为正文导航。日期无法确认时先报告，不能伪造首次收录日。
4. 逐条核对原文与用户思考。旧条目只有 `原文见` 内部链接时，先找到链接所指实际正文（包括旧 pending 路径对应的 processed 文件），完整放到最后的 `## 原始内容`；移入正文前识别归档元数据，其有来源意义的信息也要保留。原文内部的标题、代码及 Wikilink 不改写；原文从边界至 EOF 读取，不能复用旧按下一二级标题截断的逻辑。缺原文或附件时保留已有内容，登记该条未完成。
5. 原文之前及概念/索引中的 Wikilink，按实际目标转成相对 Markdown 链接，处理空格和特殊字符。必要的来源附件保留在可访问位置，调整相对引用；原文内的相对附件链接需要时保留原相对位置的附件，不为改链接重写原文。找不到的目标移除误导性导航、保留有意义文字并登记缺口，不编造概念或来源。原文和有效思考逐项与备份对照。
6. 保留有实质内容的人工概念页，将其组织为解释、关键观点、相关条目及可选相关概念；观点补上实际来源，分歧保留。移除 Dataview 等专有视图；无依据解释标明缺口，不假造支撑。仅用于示范的空页移出活动库，备份保留。
7. 将旧 `inbox/`、`reports/`、`knowledge-base.base`、知识目录级 `AGENTS.md`/`CLAUDE.md` 移出活动知识目录至备份，保留可用附件。目录说明内用户新增的有效规则承接到现有项目规则入口。仅收录用户选定的额外历史材料，不把所有收件箱和报告直接入库。
8. 依据迁移后实际条目与概念重建索引，不能套空模板。安装两个新 skill，将项目规则中指向旧入口的部分替换为上方读取规则，保留其他约定。归属不明的同名新 skill 仍按冲突流程处理。
9. 核验原文、用户思考、人工解释、附件可访问性、导航目标与代表性检索；确认新的接入可用后，将项目级旧 skill 移出活动目录至备份，退出旧运行入口。未完成项明确列出；新接入失败时不得宣称迁移完成或让两套流程同时投入使用，应暂停使用并选择恢复备份。

回退时先保留迁移后可能新增的内容，再从已验证备份恢复原目录与规则，按路径比较操作，不用整目录覆盖丢掉迁移后的用户数据。备份只用于保留和回退，不是兼容运行路径。

## 接入验收与版本依据

真实客户端接入至少确认：两个 skill 实际发现、根规则实际加载、读取正确索引；用独立临时项目做一次入库，另开无历史会话查询并附来源，再从含中文空格路径的子目录查询，确认没有错位目录。比较检索前后文件集合与内容。重复接入及模板更新须验证已有内容不变。

开发验收应记录日期、客户端版本、证据路径及未验证项，验收记录不复制到学员运行项目。静态文件校验不能替代客户端端到端验证。

官方依据：[Codex skills](https://developers.openai.com/codex/skills)、[Codex 项目规则](https://developers.openai.com/codex/guides/agents-md)、[Claude Code skills](https://code.claude.com/docs/en/skills)、[Claude Code memory](https://code.claude.com/docs/en/memory)。支持的接入目录以本文为准，版本实际加载情况以本地验收记录为准。
